@{{agent_file}}

# CLAUDE.md

Claude Code loads `CLAUDE.md` at session start.

Shared repo guidance lives in `{{agent_file}}`. Keep this router short so
Claude receives the same harness entrypoint without duplicating the project
manual.
