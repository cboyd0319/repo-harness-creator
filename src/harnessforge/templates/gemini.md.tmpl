@{{agent_file}}

# GEMINI.md

Gemini-family tools load `GEMINI.md` at session start. Antigravity can also
consume `AGENTS.md` directly.

Shared repo guidance lives in `{{agent_file}}`. Keep this router short so
Gemini-family tooling receives the same harness entrypoint without duplicating
the project manual.
