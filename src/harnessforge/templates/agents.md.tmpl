# {{agent_file}}

## Project overview

{{project_name}} uses a repo harness so coding agents can start with the right
context, stay scoped, verify work, and leave a clean restart path.

Core harness contract: instructions, tools, environment, state, and feedback
are all required. Keep instructions map-like, tool access sufficient but least
privileged, environment facts self-describing, state current across sessions,
and verification commands explicit. Feedback usually has the best return, so
fix missing or weak checks before adding broader process.
Changing instruction, tool, filesystem, git, startup, verification, hook, lint,
or evaluator surfaces changes the effective agent. Treat those as harness
product changes.

Detected stack: {{detected_stack}}
Languages: {{languages}}
Package managers: {{package_managers}}
Harness platform contract: {{platform_contract_summary}}
{{agent_file_usage_note}}
Detected project context:

{{project_context_markdown}}

Startup path:

1. Confirm the working directory.
2. Read this file and `docs/harness/README.md`.
3. Read `feature_list.json`, `progress.md`, and `session-handoff.md`.
4. Check `docs/harness/state/roadmap.md` before selecting, deferring, or reshaping
   backlog, release-prep, or product-scope work.
5. Check `docs/harness/boundaries/component-inventory.md` before changing component
   boundaries or verification routing.
6. For harness-maintenance work, use `.agents/skills/harness/SKILL.md`; it is
   the repo-local zero-install playbook for improving this harness from repo
   files.
7. For the first agent session after harness generation, review
   `docs/harness/state/first-agent-task.md` and improve the harness before unrelated
   feature work.
8. Pick one current objective before editing.

## Build and test commands

Use the smallest reliable command for the change. Current detected commands:

{{commands_markdown}}

{{verification_entrypoints_markdown}}

Prefer local verification during active work. Push or open remote CI only at an
explicit project checkpoint, release point, or user request because remote CI
has real cost.

## Code style guidelines

- Follow the repository's existing language, framework, formatting, and helper
  patterns.
- Prefer the smallest correct change. Preserve user changes and dirty work.
- One feature or fix at a time unless `docs/harness/operations/multi-agent-orchestration.md`
  defines separate owners.
- Keep project-specific facts in repo docs, not in chat history.
- Keep platform-specific instruction routers short. They should point to this
  canonical repo instruction file instead of duplicating durable rules.
- Prefer repo-relative paths. Do not commit machine-specific or user-specific
  absolute local paths unless the user explicitly requests that exact path and
  durable evidence records why.
- If the repo contains intentionally vulnerable training, demo, or fixture
  code, preserve it unless the user explicitly requests remediation for that
  scope.
- Use latest stable supported packages with hard pins. Direct dependencies use
  exact versions; external GitHub Actions use full-length commit SHAs with
  version comments.
- Keep this file as a map. Durable detail belongs in `docs/harness/`.

### Implementation Discipline

Before writing code, stop at the first rung that solves the request:

1. Do not build if the goal can be met by no change, deletion, documentation,
   configuration, or existing behavior.
2. Use the standard library before custom code.
3. Use native platform features before new libraries.
4. Use existing project dependencies before adding dependencies.
5. Prefer one clear local change over a new abstraction.
6. Only then write the minimum code that satisfies the contract.

Rules:

- State assumptions and tradeoffs before coding when the request is ambiguous.
- Do not add speculative features, configurability, abstractions, workflows, or
  dependencies.
- Keep every changed line traceable to the current objective. Avoid drive-by
  refactors, style churn, and unrelated cleanup.
- If an intentional simplification has a known ceiling, record the ceiling and
  the upgrade path in nearby code, docs, or handoff notes.
- Do not cut input validation at trust boundaries, data-loss prevention,
  security, accessibility, platform parity, or explicit user requirements.
- Non-trivial logic needs one focused runnable check. Trivial one-line edits can
  rely on the existing relevant check.

## Testing instructions

- Do not claim done without verification evidence.
- Prefer local linting, tests, pin checks, and audit before remote CI.
- Add or update focused tests for changed behavior when practical.
- For behavior changes, prefer a separate test-design step when practical.
  Reject stubbed, assertion-free, or shortcut tests, and confirm new tests fail
  before implementation when that failure is reliable and cheap to reproduce.
- Record skipped checks with reason and risk in durable state.
- Definition Of Done: target behavior or documentation change is complete,
  acceptance criteria are satisfied, relevant checks ran, and the next session
  can restart from the harness files.
- End of Session: update `progress.md` and `session-handoff.md` with current
  state, verification evidence, blockers, and the recommended next step. Use
  `docs/harness/state/clean-state-checklist.md` before claiming the session is
  complete.

## Security considerations

- People may run this repo's commands on personal machines. Choose the most
  secure and easiest behavior for edge cases. When those conflict, security wins
  and the error message must explain the safe next step.
- Do not overwrite user work or generated artifacts without explicit approval.
- Respect `docs/harness/boundaries/security-boundary-map.md` for security, privacy,
  permission, and cost boundaries.
- Respect `docs/harness/boundaries/dependency-change-policy.md` for package, tool, and
  Action pin policy.
- Do not commit secrets, credentials, private data, machine-specific absolute
  paths, or long raw command output.
