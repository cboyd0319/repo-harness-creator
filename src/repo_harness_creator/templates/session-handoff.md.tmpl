# Session Handoff

Last Updated: {{generated_date}}

## Current Objective

Replace this with the current project objective before a long session ends.

## Files

- `{{agent_file}}`
- `feature_list.json`
- `progress.md`
- `docs/harness/`

## Blockers

- None recorded.

## Next Session

Read `{{agent_file}}`, `feature_list.json`, and `progress.md`, then run the
smallest relevant verification command before making broad changes.

If this is still the initializer pass, confirm the baseline harness commit
exists before starting feature work.

Before calling the prior objective complete, compare `progress.md` against its
completion bar and record any skipped checks with owner, reason, and risk.
